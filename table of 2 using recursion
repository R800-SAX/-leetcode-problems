#include <iostream>
using namespace std;

void table(int n, int i = 1)
{
    if(i > 100)   // base condition
    {
        return;
    }

    cout << n << " x " << i << " = " << n * i << endl;

    table(n, i + 1);   // recursive call
}

int main()
{
    table(2);

    return 0;
}
